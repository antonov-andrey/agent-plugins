---
name: task-merge
description: Merge exactly one independently reviewed Linear code task in Merging, enforcing reviewed PR base commits and heads, declared branches, required checks, branch protection, ordered recovery and final links without hidden fix-forward.
---

# Merge Linear Task

Run only in a fresh thread for one code implementation already moved by an independent zero-finding Codex review from `Review` to `Merging`. Read `../../references/manual-workflow.md`, the exact provider-read review handoff, PR links, repository bases and ordered merge plan. No per-PR human approval is required by this workflow; repository branch protection remains mandatory.

Every preview or user-facing handoff MUST state that one canonical-root issue guard is acquired before dispatch/mutation, held continuously across ordered recovery, nested cleanup and final provider readback, and released only by process exit afterward. The provider handoff does not duplicate guard state.

1. Set canonical `LINEAR_AGENT_WORKSPACE_ROOT`, start `../../lib/task_workspace/tool/attempt.py hold`, require `status=held`, and keep that process alive through the boundary.
2. Before dispatch, run `scripts/git_transport.py inspect` on the exact merge host. A missing or invalid runtime stops the attempt. Host setup is the separate explicit one-time `scripts/git_transport.py provision` operation: it supports only Ubuntu 24.04 amd64, downloads one exact SHA-256-pinned Git 2.54.0 Noble package without ambient proxies, installs it atomically in a private versioned directory below the OS user's standard `HOME`, and rereads its executable build. Never provision from `scripts/pull_request.py`, during a merge attempt, or into a temporary runtime path. Fully reread Linear and run dispatch. Require active Project, exact issue `Merging`, implementation/code role, exact assignment and no blockers.
3. Reconcile attempt resources before recovering an interrupted or partial-merge path. Parse the latest provider-marked zero-finding review handoff and semantically require the candidate fields consumed by merge. Take its complete ordered composite PR candidate list as direct review evidence. Each candidate contains its URL, base branch, base commit and head commit. For every PR run `scripts/pull_request.py inspect --base-branch <base> --head-branch <head> --reviewed-base-commit <commit> --reviewed-head-commit <commit> --merge-method <method> --repository-path <exact-worktree>` for declared `merge`. Inspect immutable PR/result identity and terminal state first. `CLOSED` with no merged result is never successful merge evidence. Public terminal inspection never returns verified success from generic PR metadata: an already `MERGED` exact candidate enters strategy-specific crash recovery without reading current protection or check definitions and must prove its immutable reviewed base/head, exact merge-commit tree and ordered parents, exact REST terminal login/numeric user ID/node ID and the exact retained reviewed head. A merged `squash` or `rebase` result is unsupported and unverified until an equally exact immutable strategy proof exists. For an open PR only, the typed read binds the exact same-repository head and repository/base to the authenticated execution principal and exact write permission, rejects any existing auto-merge request, and reads the closed classic/effective-ruleset protection snapshot, every applicable active ruleset identity and bypass list, exact protected required-check definitions, and typed current results. A valid zero required-check set comes only from a successful typed status-rollup read; exit `1`, empty output, malformed output, disabled protection, missing protection or any applicable bypass is failure, never an empty/success result.
4. If source, generated output, base branch, base commit or PR head changed after review, or the exact reviewed-base CAS lease rejects, perform no merge retry against new identities. Reconcile attempt resources, then publish a minimal handoff and semantically reread the provider-marked fields required by the transition. Its human summary states that rework is required. Include the reviewed composite candidates and direct stale-state check results with applicable evidence links. Then move `Merging -> Rework`. Do not fix or rebase in merge.

Every semantic `git` command resolves to the absolute executable and matching libexec directory returned by the already-provisioned owned runtime. Closed `PATH` can never select the host's older Git, and no environment override can select another binary. Package identity and reported version are setup evidence only; the fresh first-request behavior probe of that same absolute executable remains the runtime authority.

5. Merge in declared repository order. Reject every effective merge-queue rule because deferred mutation would target a later base. Declared `merge` uses `scripts/pull_request.py merge ... --repository-path <exact-worktree> --reviewed-base-commit <commit> --reviewed-head-commit <commit> --merge-method merge`. Every merge-time Git and `gh` process receives a complete closed environment with the OS user's standard `HOME`, no `CODEX_HOME`, prompting disabled, `GIT_CONFIG_GLOBAL=/dev/null`, `GIT_CONFIG_SYSTEM=/dev/null`, disabled system attributes and no ambient authentication, config, askpass, hooks, proxy, URL rewrite, transport, replace-ref or alternate-object inputs. Git must implement `http.proactiveAuth=basic` (available in Git 2.46 and later), but version or config-key parsing is not proof: immediately before each authenticated GitHub Git boundary, run the same executable against a private loopback listener and require its invocation helper's one-time Basic credential on the first HTTP request. Failure stops before any GitHub Git request. Apply the proved setting to every fetch, push and ref readback so a foreign standard-home `.netrc` credential cannot precede the exact helper. The worktree is only a read-only audit source: capture its ordinary config bytes once without includes, require one canonical matching origin, reject unsafe local keys and substitution state, and never use that task repository for object lookup, construction, network or recovery, so a later local-config change is irrelevant. The provider creates a mode-`0700` temporary bare repository, removes its generated local config, requires absent hooks, alternates, grafts, replace refs, shallow state and `$GIT_DIR/info/attributes`, sets `core.attributesFile=/dev/null`, and fetches only the exact reviewed refs from the explicit canonical GitHub HTTPS URL. Construction requires reviewed base ancestry of reviewed head and uses the exact reviewed head tree directly; it never runs merge machinery or merge drivers. Read and strictly validate the complete relevant GitHub repository identity/status/merge-policy fields under fresh exact principal checks immediately before construction and again immediately before push, require the declared method enabled, require automatic branch deletion disabled, require both snapshots equal, and fail before ref mutation on missing, malformed, disabled, identity-conflicting or drifted policy. The sole invocation-local helper is bound to the exact HTTPS host/path, resolves the named `gh` token only for that destination, sends it to GitHub `/user` only through an stdin-fed `/usr/bin/curl -q ... --config -` header, validates the exact login, numeric user ID and node ID approved by protection/write authority, and only then emits the credential; no token enters Python, any process argv or environment, files, logs, state or evidence. Absolute `/usr/bin/curl` and `/usr/bin/jq` are runtime prerequisites alongside behaviorally supported Git 2.46 or later. The push uses an inert hooks path and `--no-verify`, leases only the exact reviewed old base OID, and publishes the exact prepared two-parent no-fast-forward merge commit only to the base ref. The PR head ref remains unchanged and open through this mutation. GitHub cannot atomically lease REST repository metadata in the Git receive-pack transaction, so the second policy read closes observed drift but leaves an unavoidable narrow policy-change race before push; never describe policy and ref mutation as one atomic provider operation. Classic or effective-ruleset protection must prohibit non-fast-forward updates and base deletion for the executing identity without bypass, and its exact required-check definition set must be zero because the new merge commit cannot have checks before push. Immediate ref readback must prove the exact prepared merge at base and the exact reviewed commit still at head. GitHub must then expose the PR as `MERGED`, with the exact terminal provider principal and immutable Git tree/ordered-parent proof, before the merge is successful. `CLOSED` without merged state is failure. If ref readback succeeded but terminal PR state lags, use native waiting and retry only exact crash recovery; recovery uses a new identically closed private repository and proves reviewed ancestry, exact head tree, ordered reviewed parents, terminal REST identity and the exact retained head without depending on later protection or policy. Declared `squash` and `rebase` fail closed before mutation because no equally exact immutable result proof exists. Retry only transient operations proved not to change reviewed identity. Never hide or roll back a partial cross-repository merge; reconcile attempt resources before publishing completed merges and exact remaining recovery order.
Each individual authenticated GitHub fetch, push and ref-readback command in normal merge and recovery MUST have its own fresh successful behavioral proactive-authentication probe immediately before that command. One probe authorizes exactly one following network Git command; a failed probe stops before that command.

6. Use native background-terminal waiting for provider operations. Do not create model polling, a Project supervisor, command timeouts, arbitrary thresholds or an alternate Codex home. If the harness launches a child, use the operating-system user's standard `HOME`, remove `CODEX_HOME`, pass one complete prompt and close stdin in the same launch operation, and do not forward Linear credentials.
7. When the merged candidate changes the installed `linear-agent-tools` plugin that owns this lifecycle, its reviewed source MUST already contain the new cachebuster produced by the normal plugin-creator update flow. Only after exact terminal GitHub/Git readback of that candidate, and while its reviewed task worktree is retained, run branch-local `scripts/provider_install.py --issue-identifier <issue> --base-branch <base> --reviewed-base-commit <old-base> --reviewed-head-commit <head> --merged-base-commit <merge> --expected-version <version>` under standard `HOME` with `CODEX_HOME` unset. This fixed command is the narrow bootstrap authority until normal installation completes: it validates its own exact reviewed head, discovers the unique configured local marketplace from the fixed plugin and repository identity, accepts its clean base worktree only at the reviewed old base or exact merged base, validates one explicit fetch destination, fetches and proves the remote exact two-parent merge/tree, and fast-forwards only old base to merged base through the shared closed Git invocation boundary. Every authority-bearing Git read and mutation uses that same complete minimal standard-user owner, ignores global/system and invocation Git config, disables replacement and alternate object resolution and hooks, and enables only the exact validated destination protocol and standard-home credentials it needs. The command invokes ordinary `codex plugin add linear-agent-tools@<discovered-marketplace> --json` only when exact merged cache/list readback is incomplete. It then rereads the marketplace source HEAD and manifest, installed version, enabled state, and every merged tracked plugin file in the versioned standard-home cache. The cache is installation readback, never source authority. Rerun the same command after a crash; it derives the first incomplete phase from Git and Codex state and creates no receipt, journal or compatibility state.
8. The installation command returns one complete `discovery_prompt` and exact `expected_discovery_result`. Because this lifecycle-provider change requires fresh discovery, first revalidate the canonical `LINEAR_AGENT_WORKSPACE_ROOT` already bound in step 1, then have the outer harness directly launch a new generic `gpt-5.6-sol` max `codex exec --skip-git-repo-check` process with that non-Git root as its non-provider working directory and a read-only sandbox. Pass the prompt as one argument, close stdin in the same launch, preserve the OS user's standard `HOME`, remove `CODEX_HOME` and Linear credentials, and wait through native process completion. Do not add a launcher wrapper, polling loop, timeout, supervisor or `--ephemeral`. The prompt names no skill and supplies no expected skill list: the fresh process activates no skill, selects availability only from initial catalog locators under the exact installed version directory, reads the installed/source manifests and returns one exact JSON object. Parse its sole JSON result and semantically require exact equality with `expected_discovery_result`; structured `ready=false`, prose, extra output, failure or ambiguity stops before `Done`. The installed/cached provider and branch-local provider do not certify their own discovery.
9. Only after every candidate is directly read back as `MERGED` with its exact provider/Git proof and any required provider install plus fresh discovery readback is complete, reconcile attempt resources under the live guard. Publish and semantically reread one minimal provider-marked handoff whose human summary states the merge result. Keep each reviewed composite PR candidate and add its exact merged commit. Include direct check results with applicable evidence links. Include any nonempty subset of directly exposed exact known Codex usage counters. Validate `Merging -> Done` and mutate/reread Linear. Only then run ordinary idempotent issue-owned cleanup under the same guard; that cleanup exclusively owns deletion of the retained task branch. Never delete a branch attached to an open PR. Do not complete downstream graph review or acceptance automatically.

Attempt-resource cleanup is a mandatory pre-handoff and pre-transition step for success, finding, stale-state, failed, canceled and controlled interrupted outcomes. No status mutation or handoff publication may precede it. An abrupt process loss publishes nothing; the next guarded attempt repeats the idempotent cleanup before recovery.

No verification receipt, candidate fingerprint or generic invalidation gate is created. Delete transient inputs only after final provider readback; process exit releases the guard.
