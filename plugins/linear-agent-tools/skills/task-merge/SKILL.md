---
name: task-merge
description: Merge exactly one independently reviewed Linear code task in Merging, enforcing reviewed PR base commits and heads, declared branches, required checks, branch protection, ordered recovery and final links without hidden fix-forward.
---

# Merge Linear Task

Run only in a fresh thread for one code implementation already moved by an independent zero-finding Codex review from `Review` to `Merging`. Read `../../references/manual-workflow.md`, the exact provider-read review handoff, PR links, repository bases and ordered merge plan. No per-PR human approval is required by this workflow; repository branch protection remains mandatory.

Every preview or handoff MUST state that one canonical-root issue guard is acquired before dispatch/mutation, held continuously across ordered recovery, nested cleanup and final provider readback, and released only by process exit afterward.

1. Set canonical `LINEAR_AGENT_WORKSPACE_ROOT`, start `../../lib/task_workspace/tool/attempt.py hold`, require `status=held`, and keep that process alive through the boundary.
2. Fully reread Linear and run dispatch. Require active Project, exact issue `Merging`, implementation/code role, exact assignment and no blockers.
3. Reconcile attempt resources before recovering an interrupted or partial-merge path. Parse the latest byte-for-byte provider-read `review-passed` handoff and take its complete PR URL/base-branch/base-commit/head map as direct review evidence. For every PR run `scripts/pull_request.py inspect --base-branch <base> --head-branch <head> --reviewed-base-commit <commit> --reviewed-head-commit <commit> --merge-method <method> --repository-path <exact-worktree>` for declared `merge`. Inspect immutable PR/result identity and terminal state first. Public terminal inspection never returns verified success from generic PR metadata: an already merged exact candidate enters strategy-specific crash recovery without reading current protection or check definitions and must prove its immutable reviewed base/head, exact merge-commit tree and ordered parents, exact REST terminal login/numeric user ID/node ID and exact deleted reviewed head. A merged `squash` or `rebase` result is unsupported and unverified until an equally exact immutable strategy proof exists. For an open PR only, the typed read binds the exact same-repository head and repository/base to the authenticated execution principal and exact write permission, rejects any existing auto-merge request, and reads the closed classic/effective-ruleset protection snapshot, every applicable active ruleset identity and bypass list, exact protected required-check definitions, and typed current results. A valid zero required-check set comes only from a successful typed status-rollup read; exit `1`, empty output, malformed output, disabled protection, missing protection or any applicable bypass is failure, never an empty/success result.
4. If source, generated output, base branch, base commit or PR head changed after review, or an atomic ref lease rejects because either ref changed after preflight, perform no merge retry against new identities. Reconcile attempt resources, publish and byte-for-byte reread a `rework-required` semantic handoff with direct stale-state evidence, then move `Merging -> Rework`. Do not fix or rebase in merge.
5. Merge in declared repository order. Reject every effective merge-queue rule because deferred mutation would target a later base. Declared `merge` uses `scripts/pull_request.py merge ... --repository-path <exact-worktree> --reviewed-base-commit <commit> --reviewed-head-commit <commit> --merge-method merge`. Every merge-time Git and `gh` process receives a complete closed environment with the OS user's standard `HOME`, no `CODEX_HOME`, prompting disabled and no ambient authentication, config, hooks, proxy, URL rewrite, transport, replace-ref or alternate-object inputs. Git reads no system, global or local config during object or network operations. A read-only local config-name audit rejects authorization/extra headers, askpass, helpers, hooks, proxies, URL rewrites, transport redirection, merge drivers and object substitution; the repository also rejects pre-push hooks, alternates, grafts, replace refs and shallow history. The transaction requires one canonical matching configured origin but always uses one explicit canonical GitHub HTTPS destination. Its sole invocation-local helper resolves the named `gh` token internally, validates that actual token against GitHub `/user`, requires the exact login, numeric user ID and node ID approved by the protection/write-authority snapshot, and only then emits the credential; no token enters Python, argv, logs, state or evidence. The push uses an inert hooks path and `--no-verify`, constructs the no-fast-forward merge locally, and applies one server-side atomic transaction that leases the exact reviewed base and head while updating base and deleting head. Either old OID mismatch rejects both updates. Classic or effective-ruleset protection must prohibit non-fast-forward updates and base deletion for the executing identity without bypass, and its exact required-check definition set must be zero because the newly constructed merge commit cannot have provider checks before the atomic push. If the atomic remote readback succeeded but GitHub has not yet exposed terminal PR state, use native waiting and retry only the exact strategy-specific crash-recovery read; later protection/check drift and later base advancement do not invalidate the immutable result, while the exact reviewed head, merge tree, ordered reviewed parents, REST terminal identity and deleted head remain mandatory. Declared `squash` and `rebase` fail closed before mutation because no equally exact immutable result proof is implemented. Retry only transient operations proved not to change reviewed identity. Never hide or roll back a partial cross-repository merge; reconcile attempt resources before publishing completed merges and exact remaining recovery order.
6. Use native background-terminal waiting for provider operations. Do not create model polling, a Project supervisor, command timeouts, arbitrary thresholds or an alternate Codex home.
7. After all merges are directly read back, reconcile attempt resources under the live guard and publish/reread one `merged` semantic handoff with reviewed PR base branches, base commits and heads, final commits, evidence URLs and any nonempty subset of directly exposed exact known Codex usage counters. Validate `Merging -> Done`, mutate/reread Linear, then run issue-owned cleanup under the same guard. Do not complete downstream graph review or acceptance automatically.

Attempt-resource cleanup is a mandatory pre-handoff and pre-transition step for success, finding, stale-state, failed, canceled and controlled interrupted outcomes. No status mutation or handoff publication may precede it. An abrupt process loss publishes nothing; the next guarded attempt repeats the idempotent cleanup before recovery.

No verification receipt, candidate fingerprint or generic invalidation gate is created. Delete transient inputs only after final provider readback; process exit releases the guard.
