interface:
  display_name: "Merge Linear Task"
  short_description: "Merge one independently reviewed Linear task"
  default_prompt: "Use $task-merge to merge this independently reviewed Linear issue."
