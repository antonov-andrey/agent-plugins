interface:
  display_name: "Approved Instruction Ownership Migration"
  short_description: "Move instruction owners without semantic loss."
  default_prompt: "Use $instruction-migration to apply the approved source-to-target instruction ledger."
policy:
  allow_implicit_invocation: false
